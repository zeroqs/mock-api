# Инструкция по развертыванию QA Admin

## ⚙️ Установка и запуск

### 1. Установка зависимостей

```bash
npm ci
```

### 2. Настройка базы данных

```bash
npx drizzle-kit push --config=drizzle-prod.config.ts
```

### 3. Сборка и запуск

```bash
npm run build
npm run prod
```

## 🔄 CI/CD Pipeline

### Этапы пайплайна

1. **Lint** — проверка кода (`npm run lint`)
2. **Build** — сборка приложения (`npm run build`)

### Команды для CI

```bash
npm ci
npx drizzle-kit push --config=drizzle-prod.config.ts
npm run build
```

## ⚠️ Важные замечания

- Файл БД должен быть доступен для записи
