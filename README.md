# QA Admin - Mock API Management System

Централизованная система управления mock API эндпоинтами для тестирования и разработки.

## 📋 Описание

QA Admin — это веб-приложение для создания и управления mock API эндпоинтами. Сервис позволяет создавать виртуальные API эндпоинты с настраиваемыми ответами, что упрощает разработку и тестирование фронтенд-приложений без необходимости подключения к реальным бэкенд-сервисам.

## ✨ Основные возможности

### Управление эндпоинтами

- **Создание эндпоинтов** — создание mock эндпоинтов с указанием HTTP метода (GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS) и пути
- **Редактирование эндпоинтов** — изменение параметров существующих эндпоинтов
- **Фильтрация** — поиск эндпоинтов по пути и фильтрация по HTTP методам
- **Удаление** — удаление эндпоинтов вместе со всеми связанными пресетами

### Управление пресетами

- **Множественные пресеты** — создание нескольких вариантов ответов для одного эндпоинта
- **Активный пресет** — переключение между пресетами через radio buttons
- **Настраиваемые ответы** — задание HTTP статус-кода и JSON данных для ответа
- **Фильтрация по query параметрам** — автоматическая фильтрация данных ответа на основе query параметров запроса

### Mock API

- **Эмуляция реальных API** — все запросы к `/api/mock/*` обрабатываются и возвращают настроенные ответы
- **Поддержка всех HTTP методов** — полная поддержка стандартных HTTP методов
- **Динамическая фильтрация** — фильтрация ответов на основе query параметров (например, `?color=red,blue`)

### API для интеграции

- **GET `/api/endpoints`** — получение списка активных эндпоинтов в формате `{ method, path }`

## 🛠 Технологии

- **Next.js 16** — React фреймворк с App Router
- **TypeScript** — типизированный JavaScript
- **Drizzle ORM** — легковесная ORM для работы с базой данных
- **SQLite** — легковесная база данных
- **Mantine UI** — компонентная библиотека для UI
- **Zod** — валидация схем данных
- **React Server Actions** — серверные действия для работы с данными

## 🚀 Установка и запуск

### Требования

- Node.js 18+
- npm или yarn

### Установка зависимостей

```bash
npm install
```

### Настройка базы данных

1. Примените миграции для разработки:

```bash
npx drizzle-kit push --config=drizzle-dev.config.ts
```

Или для продакшена:

```bash
npx drizzle-kit push --config=drizzle-prod.config.ts
```

3. (Опционально) Заполните базу тестовыми данными:

```bash
npm run seed
```

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка для продакшена

```bash
npm run build
npm run prod
```

## 📁 Структура проекта

```
qa-admin/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/               # API роуты
│   │   │   ├── endpoints/     # Управление эндпоинтами
│   │   │   ├── mock/          # Mock API роуты
│   │   │   └── presets/       # Управление пресетами
│   │   ├── @modal/            # Parallel Routes для модалок
│   │   ├── endpoints/         # Страницы редактирования
│   │   └── page.tsx           # Главная страница
│   └── shared/
│       ├── lib/               # Утилиты и серверные действия
│       └── ui/                # UI компоненты
├── drizzle/
│   ├── db.ts                  # Инициализация Drizzle клиента
│   └── migrations/            # Миграции БД
│       ├── schema.ts          # Схема базы данных
│       ├── relations.ts       # Реляции между таблицами
│       └── *.sql              # SQL миграции
├── drizzle-dev.config.ts      # Конфигурация Drizzle для разработки
├── drizzle-prod.config.ts     # Конфигурация Drizzle для продакшена
└── scripts/
    └── seed-test-data.ts      # Скрипт для заполнения тестовыми данными
```

## 🗄 Модели данных

### MockEndpoint

- `id` — уникальный идентификатор
- `method` — HTTP метод (GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS)
- `path` — путь эндпоинта (например, `/api/users`)
- `description` — описание эндпоинта (опционально)
- `presets` — связанные пресеты

### MockPreset

- `id` — уникальный идентификатор
- `name` — название пресета
- `enabled` — флаг активности (только один пресет может быть активным)
- `statusCode` — HTTP статус-код ответа (по умолчанию 200)
- `responseData` — JSON данные для ответа
- `filterKeys` — массив ключей для фильтрации по query параметрам (опционально)

## 🔌 API эндпоинты

### Mock API

Все запросы к `/api/mock/*` обрабатываются как mock эндпоинты.

**Пример:**

```bash
# Если создан эндпоинт GET /api/users
GET http://localhost:3000/api/mock/api/users

# С фильтрацией по query параметрам
GET http://localhost:3000/api/mock/api/users?status=active&role=admin
```

### Управление эндпоинтами

**GET `/api/endpoints`**
Возвращает список активных эндпоинтов.

**Ответ:**

```json
[
  { "method": "GET", "path": "/api/users" },
  { "method": "POST", "path": "/api/items" }
]
```

## 💡 Примеры использования

### Создание эндпоинта с пресетом

1. Откройте главную страницу
2. Нажмите "Создать эндпоинт"
3. Заполните форму:
   - Метод: `GET`
   - Путь: `/api/users`
   - Описание: `Получение списка пользователей`
4. Добавьте пресет:
   - Название: `Успешный ответ`
   - Статус: `200`
   - Данные: `[{ "id": 1, "name": "John" }]`
   - Ключи фильтрации: `["status", "role"]` (опционально)
5. Сохраните

### Использование фильтрации

Если в пресете указаны `filterKeys: ["status", "role"]`, то запрос:

```
GET /api/mock/api/users?status=active&role=admin
```

Вернет только те элементы массива, у которых `status === "active"` и `role === "admin"`.

Поддерживаются множественные значения через запятую:

```
GET /api/mock/api/users?status=active,pending
```

## 🔧 Скрипты

- `npm run dev` — запуск в режиме разработки
- `npm run build` — сборка для продакшена
- `npm run prod` — запуск продакшен версии
- `npm run lint` — проверка кода линтером
- `npm run format` — форматирование кода
- `npm run seed` — заполнение базы данных тестовыми данными

## 📝 Лицензия

Приватный проект
